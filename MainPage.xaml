<Page
    x:Class="HelloBoard.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HelloBoard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}">

    <Canvas>
        <Grid VerticalAlignment="Top" Width="1270" Height="720">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <MenuBar HorizontalAlignment="Stretch" VerticalAlignment="Top">
                <MenuBarItem Title="Arquivo">
                    <MenuFlyoutItem Text="Novo quadro"/>
                    <MenuFlyoutItem Text="Abrir"/>
                    <MenuFlyoutItem Text="Fechar"/>
                </MenuBarItem>
                <MenuBarItem Title="Editar">
                    <MenuFlyoutItem Text="Adicionar cartão"/>
                    <MenuFlyoutItem Text="Copiar texto"/>
                    <MenuFlyoutItem Text="Substituir texto"/>
                </MenuBarItem>
                <MenuBarItem Title="Exibir">
                    <MenuFlyoutItem Text="Número de colunas"/>
                </MenuBarItem>
                <MenuBarItem Title="Ajuda">
                    <MenuFlyoutItem Text="Sobre"/>
                </MenuBarItem>
            </MenuBar>
            <Grid x:Name="gdBoard" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel x:Name="spCol1"/>
                <StackPanel x:Name="spCol2" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                <StackPanel x:Name="spCol3" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </Grid>
        </Grid>
    </Canvas>
</Page>
